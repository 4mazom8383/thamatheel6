var myScroll;

function loaded() {
    var wrapper = document.getElementById('wrapper');
    var myScroll = new IScroll(wrapper,{

        mouseWheel: true,
        scrollbars: true,
        mouseWheel: true

    });
};